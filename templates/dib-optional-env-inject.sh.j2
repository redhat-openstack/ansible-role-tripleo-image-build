#!/bin/bash

# This is a generated file used to configure DIB.  It is sourced just prior
# to calls to disk-image-create
{#
  Additional env variables used by DIB (such as DIB_YUM_REPO_CONF) can be
  defined in  dict() artib_dib_environment_additional.  They will be emitted
  to a script that is sourced prior to running dib.
#}

{# Lay down base env
{% if artib_dib_environment is defined %}
{% for varname, varval in artib_dib_environment.iteritems() %}
export {{ varname }}="{{ varval }}"
{% endfor %}
{% endif %}
#}

export DIB_DEFAULT_INSTALLTYPE=package
export ELEMENTS_PATH="{{ dib_elements_path }}"


{# Additional env to enable --extra-vars simple debugging.  Note: these can override defaults #}
{% if artib_dib_environment_additional is defined %}
{% for varname, varval in artib_dib_environment_additional.iteritems() %}
export {{ varname }}="{{ varval }}"
{% endfor %}
{% endif %}

